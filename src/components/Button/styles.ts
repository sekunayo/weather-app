import { css } from "@emotion/css";

const button = css({
    width: "fit-content",
    height: "40px",
    padding: "10px 18px",
    color: "#E7E7EB",
    fontSize: "16px",
    fontStyle: "normal",
    fontWeight: 500,
    lineHeight: "normal",
    backgroundColor: "#6E707A",
    boxShadow: "0px 4px 4px 0px rgba(0, 0, 0, 0.25)"
})

export const styles = {
    button
}